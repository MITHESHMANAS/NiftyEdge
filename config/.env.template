# Copy this file to .env in the project root and adjust as needed:
#   cp config/.env.template .env

# Copy this file to .env in the project root and adjust as needed:
#   cp config/.env.template .env
#
# All paths may be relative (resolved against the project root) or absolute.

DB_PATH=data/nifty100.db
RAW_DATA_PATH=data/raw
SUPPLEMENTARY_DATA_PATH=data/raw/supporting_datasets
OUTPUT_PATH=output
LOG_PATH=logs
SCHEMA_PATH=db/schema.sql

PORT=8000
LOG_LEVEL=INFO

# Set to true to enable live HTTP validation of documents.xlsx URLs (DQ-13).
# Off by default: 1,585 URLs x network round-trip is slow and not needed
# for a normal ETL run.
VALIDATE_URLS=false

